-- 1. Create the table
2E3MEL GADWAL students (id RAKAM, name GOMLA, gpa KASR);

-- 2. Create an Index on GPA (so we can search fast)
2E3MEL FEHRIS idx_gpa 3ALA students (gpa);

-- 3. Insert Data
EMLA GOWA students ELKEYAM (1, 'Mohamed', 3.8);
EMLA GOWA students ELKEYAM (2, 'Sara', 4.0);
EMLA GOWA students ELKEYAM (3, 'Ahmed', 2.5);

-- 4. Check it
2E5TAR * MEN students;

-- 5. QUIT (This tests persistence)
exit


-- 6. Query with Index (Optimizer should trigger)
2E5TAR * MEN students LAMA gpa = 4.0;









-- Create table with PRIMARY KEY
2E3MEL GADWAL users (id RAKAM ASASI, name GOMLA, email GOMLA);

-- Begin transaction
2EBDA2;

-- Insert (will be tracked for rollback)
EMLA GOWA users ELKEYAM (1, 'Ahmed', 'ahmed@example.com');

-- Rollback (undoes the insert)
2ERGA3;

-- Or commit
KAMEL;