1) create config file
2) created page structure 
3) created disk manager for I/O operations
PHASE 1 COMPLETED ( DISK MANAGER )
4) created buffer manager for caching pages in memory
PHASE 2 COMPLETED ( BUFFER MANAGER )
5) created pages layout for b+ trees
6) implemented b+ tree insertions 
7) implemented b+ tree splitting
PHASE 3 COMPLETED ( B+ TREE INDEXING )
8) created table page structure
9) implemented table heap file operations ( insert, delete, update, select )
PHASE 4 COMPLETED ( TABLE HEAP FILE )
10) Create Types for different data types ( int, float, string )
11) Implement column schema structure
12) Dynamic tuple structure
PHASE 5 COMPLETED ( SYSTEM CATALOGS AND TUPLES )
13) implemented a bit map to recycle free pages
14) implemented a free page manager to track free pages
PHASE 5.5 COMPLETED ( FREE PAGE MANAGEMENT )
15) implemented a FQL LEXER to tokenize the input query
PHASE 6 COMPLETED ( FQL LEXER )
16) implemented a FQL PARSER to parse the tokenized query into an AST 
17) insured parser is strict and follows the grammar rules
PHASE 7 COMPLETED ( FQL PARSER  )
18) implemented a FQL Executors to execute the parsed AST and perform operations on the database
19) Implemented a FQL Executor engine to handle different query types ( SELECT, INSERT, UPDATE, DELETE )
PHASE 8 COMPLETED ( FQL Executor  )
20) updated executor to interact with B+ tree indexes for faster data retrieval
21) implemented index creation and deletion commands in FQL
PHASE 9 COMPLETED ( INDEX INTEGRATION  )
22) created a simple command line interface to interact with the database
23) implemented error handling and user feedback in the CLI
PHASE 10 COMPLETED ( COMMAND LINE INTERFACE  )
24) wrote unit tests for all major components ( Disk Manager, Buffer Manager, B+ Tree
25) Created server and client architecture for remote database access
PHASE 11 COMPLETED ( TESTING AND SERVER-CLIENT ARCHITECTURE  )
26) added system tables to track metadata about user tables and indexes
27) added multi db support to allow users to create and manage multiple databases
PHASE 12 COMPLETED ( MULTI DATABASE SUPPORT AND SYSTEM TABLES  )
28) added permission management to control user access to databases and tables
29) Added crash handling and recovery mechanisms to ensure data integrity
PHASE 13 COMPLETED ( PERMISSION MANAGEMENT AND CRASH RECOVERY  )



